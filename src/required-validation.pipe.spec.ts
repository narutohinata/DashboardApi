import { RequiredValidationPipe } from './required-validation.pipe';

describe('RequiredValidationPipe', () => {
  it('should be defined', () => {
    expect(new RequiredValidationPipe()).toBeDefined();
  });
});
