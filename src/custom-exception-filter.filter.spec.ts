import { CustomExceptionFilterFilter } from './custom-exception-filter.filter';

describe('CustomExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new CustomExceptionFilterFilter()).toBeDefined();
  });
});
